plugins {
    id 'com.mydata.spring-library-conventions'
    id 'org.springframework.boot'
}

dependencies {
    api 'org.springframework.boot:spring-boot-starter-test'
}

bootJar {
    enabled = false
}